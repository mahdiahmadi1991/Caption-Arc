# Meta Workspace And Separate Repos Migration

This active execution plan moved to the private workspace-control repository because it governs the full multi-repo split and should no longer live canonically in the public extension repo.

The public extension repo keeps only this pointer.
