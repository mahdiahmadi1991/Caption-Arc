# Business KPI Framework

This product-wide KPI framework moved to the private workspace-control repository.

The public extension repo keeps only this pointer because KPI ownership is now broader than the extension repo alone.
