# Diagrams

Use this folder for architecture diagrams (Mermaid or image assets) that map to architecture docs.

Conventions:

- keep one diagram per concern (runtime, storage, message flow, release flow)
- link each diagram from the relevant architecture page
- avoid machine-specific details in labels
